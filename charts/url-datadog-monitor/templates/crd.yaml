{{- if and (eq .Values.mode "operator") .Values.operator.createCRD -}}
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: urlmonitors.urlmonitoring.kuskoman.github.com
  labels:
    {{- include "url-datadog-monitor.labels" . | nindent 4 }}
spec:
  group: urlmonitoring.kuskoman.github.com
  names:
    kind: URLMonitor
    listKind: URLMonitorList
    plural: urlmonitors
    singular: urlmonitor
  scope: Namespaced
  versions:
    - name: v1
      schema:
        openAPIV3Schema:
          description: URLMonitor is the Schema for the urlmonitors API
          properties:
            apiVersion:
              description: 'APIVersion defines the versioned schema of this representation
                of an object. Servers should convert recognized schemas to the latest
                internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
              type: string
            kind:
              description: 'Kind is a string value representing the REST resource this
                object represents. Servers may infer this from the endpoint the client
                submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
              type: string
            metadata:
              type: object
            spec:
              description: URLMonitorSpec defines the desired state of URLMonitor
              properties:
                url:
                  description: URL to monitor
                  type: string
                method:
                  description: HTTP method to use
                  type: string
                  default: GET
                interval:
                  description: Check interval in seconds
                  type: integer
                  default: 60
                timeout:
                  description: Request timeout in seconds
                  type: integer
                  default: 10
                checkCert:
                  description: Whether to check SSL certificate
                  type: boolean
                  default: true
                verifyCert:
                  description: Whether to verify SSL certificate against CA
                  type: boolean
                  default: false
                headers:
                  description: HTTP headers to include in the request
                  type: object
                  additionalProperties:
                    type: string
                labels:
                  description: Labels to attach to metrics
                  type: object
                  additionalProperties:
                    type: string
              required:
                - url
              type: object
            status:
              description: URLMonitorStatus defines the observed state of URLMonitor
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
{{- end }}